rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Only authenticated teachers can read & write students and attendance records
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
